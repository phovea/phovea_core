define(["require", "exports", 'wrapper'], function (require, exports, wrapper) {
  exports.test = function(){

    QUnit.module('wrapper', function() {

      QUnit.test('Object.keys', function(assert) {
        assert.deepEqual(Object.keys(wrapper).sort(), [
          // TODO
        ]);
      });

    });

  }
});
