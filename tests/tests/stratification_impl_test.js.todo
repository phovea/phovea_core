define(["require", "exports", 'stratification_impl'], function (require, exports, stratification_impl) {
  exports.test = function(){

    QUnit.module('stratification_impl', function() {

      QUnit.test('Object.keys', function(assert) {
        assert.deepEqual(Object.keys(stratification_impl).sort(), [
          // TODO: tsc fails currently
        ]);
      });

    });

  }
});
