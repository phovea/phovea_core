define(["require", "exports", 'mapper'], function (require, exports, mapper) {
  exports.test = function(){

    QUnit.module('mapper', function() {

      QUnit.test('Object.keys', function(assert) {
        assert.deepEqual(Object.keys(mapper).sort(), [
          // TODO
        ]);
      });

    });

  }
});
